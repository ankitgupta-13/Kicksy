import React from "react";

const RequestProduct = () => {
  return (
    <div style={{
      width: "100%",
      height: "80vh",
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      fontSize: "2rem",
      color: "#aaa"
    }}>
      No requests to show
    </div>
  );
};

export default RequestProduct;
